// four-layers.md names socketPathLabel as THE documented false positive. The
// needle is case-sensitive and word-anchored, so neither of these matches.
const socketPathLabel = 'db';
const rawSocketLabel = 'unused';
